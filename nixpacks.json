{
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_22"]
    },
    "install": {
      "cmds": ["npm ci --include=dev"]
    },
    "build": {
      "cmds": ["npm run build"]
    }
  },
  "start": {
    "cmd": "node .output/server/index.mjs"
  }
}
